interface Initial {
  isTranslate: boolean,
  areButtons: boolean,
  isOpen: boolean,
}
const initialState: Initial = {
  isTranslate: true,
  areButtons: true,
  isOpen: false,
};

export {initialState};